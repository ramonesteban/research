<div class="container">
  <h3 class="text-muted">Eco Scan</h3>

  <div class="jumbotron">
    <h2>Verify product</h2>
    <p class="text-justify">Take an image from your mobile of the label of a product to check if this is considered ecological or not.</p>

    <% if !notice.nil? %>
      <div class="alert alert-success">
        <a class="close" data-dismiss="alert" href="#" aria-hidden="true">&times;</a>
        <%= notice %>
      </div>
    <% end %>

    <% if !alert.nil? %>
      <div class="alert alert-danger">
        <a class="close" data-dismiss="alert" href="#" aria-hidden="true">&times;</a>
        <%= alert %>
      </div>
    <% end %>

    <%= form_tag(upload_picture_path, multipart: true, class: 'form-horizontal') do %>
      <div class="form-group">
        <div class="col-sm-12 text-center">
          <a class="btn btn-success" href="javascript:;">
            Take a picture ...
            <%= file_field_tag :picture, class: 'form-file-input', onchange: '$("#upload-file-info").html($(this).val());', accept: 'image/*', capture: 'camera' %>
          </a>
          <div class="form-file-span">
            <span class="label label-info" id="upload-file-info"></span>
          </div>
        </div>
      </div>
    <% end %>

    <div class="well well-sm" id="text-container" style="display: none;"></div>

    <div class="thumbnail" id="preview-container" style="display: none;">
      <h4 class="text-center">Preview</h4>
      <img id="preview-image" src="#">
    </div>
  </div>

  <div class="footer">
    <p>&copy; Ramón González 2014</p>
  </div>
</div>
