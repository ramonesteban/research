== README

=== Requirements
* Ruby version <tt>2.0</tt>

* Rails version <tt>4.0</tt>


=== Configuration

* For the database creation just run:

    $ rake db:migrate

* For start the server in development mode run:

    $ rails s
